version: "3.8"

networks:
  gizmo-net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.90.0.0/24

services:

  gizmo-llama:
    image: gizmo-llama:latest
    container_name: gizmo-llama
    networks:
      - gizmo-net
    ports:
      - "8080:8080"
    volumes:
      - ./models:/models:ro,Z
    devices:
      - nvidia.com/gpu=all
    security_opt:
      - label=disable
    command: >
      --model /models/Huihui-Qwen3.5-9B-abliterated.Q8_0.gguf
      --host 0.0.0.0
      --port 8080
      --ctx-size 32768
      --n-gpu-layers 99
      --flash-attn on
      --threads 8
      --parallel 4
      --cont-batching
      --chat-template-file /models/chat_template.jinja
      --log-disable
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  gizmo-whisper:
    image: docker.io/fedirz/faster-whisper-server:latest-cpu
    container_name: gizmo-whisper
    networks:
      - gizmo-net
    ports:
      - "8200:8000"
    environment:
      - WHISPER__MODEL=Systran/faster-whisper-large-v3
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  gizmo-searxng:
    image: docker.io/searxng/searxng:latest
    container_name: gizmo-searxng
    networks:
      - gizmo-net
    ports:
      - "8300:8080"
    volumes:
      - ./services/searxng/config:/etc/searxng:rw,Z
    environment:
      - SEARXNG_BASE_URL=http://localhost:8300
    restart: unless-stopped

  gizmo-tts:
    build:
      context: ./services/tts
      dockerfile: Dockerfile
    image: gizmo-tts:latest
    container_name: gizmo-tts
    networks:
      - gizmo-net
    ports:
      - "8400:8400"
    volumes:
      - ./models/qwen3-tts:/models/qwen3-tts:ro,Z
    devices:
      - nvidia.com/gpu=all
    security_opt:
      - label=disable
    environment:
      - TTS_MODEL_DIR=/models/qwen3-tts/1.7B-Base
      - TTS_TOKENIZER_DIR=/models/qwen3-tts/tokenizer
      - TTS_IDLE_UNLOAD_SECONDS=60
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8400/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  gizmo-orchestrator:
    build:
      context: ./services/orchestrator
      dockerfile: Dockerfile
    image: gizmo-orchestrator:latest
    container_name: gizmo-orchestrator
    networks:
      - gizmo-net
    ports:
      - "9100:9100"
    volumes:
      - ./config:/app/config:ro,Z
      - ./memory:/app/memory:rw,Z
      - ./logs:/app/logs:rw,Z
    environment:
      - LLAMA_HOST=gizmo-llama
      - LLAMA_PORT=8080
      - WHISPER_HOST=gizmo-whisper
      - WHISPER_PORT=8000
      - SEARXNG_HOST=gizmo-searxng
      - SEARXNG_PORT=8080
      - TTS_HOST=gizmo-tts
      - TTS_PORT=8400
    depends_on:
      - gizmo-llama
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9100/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  gizmo-ui:
    build:
      context: ./services/ui
      dockerfile: Dockerfile
    image: gizmo-ui:latest
    container_name: gizmo-ui
    networks:
      - gizmo-net
    ports:
      - "3100:3100"
    environment:
      - ORCHESTRATOR_URL=http://gizmo-orchestrator:9100
    depends_on:
      - gizmo-orchestrator
    restart: unless-stopped
